services:
  web:
    build:
      context: blog
      target: dev-envs
    ports:
      - 4200:4200
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./blog:/project
      - /project/node_modules
